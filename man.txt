1. У файлі install.sh встановлюється, налаштовується та запускається Jenkins.
   Створюється юзер з логіном admin та паролем 1 та запускається наступний скрипт plugin.sh.

2. У файлі plugin.sh потрібно змінити ip адресу, юзера та пароль.

3. Далі запускається файл cred.sh у ньому потрібно змінити лише gmail.
   Цей файл створює ssh ключі, приватний для jenkins(та додає його до глобальних jenkins кредів),
   та публіний для github(і показує його у командному рядку)

4. 
